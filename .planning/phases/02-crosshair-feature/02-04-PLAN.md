---
phase: 02-crosshair-feature
plan: 04
type: execute
wave: 2
depends_on: [02-02, 02-03]
files_modified:
  - src/features/crosshair/CrosshairDesigner.tsx
  - src/features/crosshair/index.ts
  - src/App.tsx
  - src/features/crosshair/__tests__/CrosshairDesigner.test.tsx
autonomous: false
requirements:
  - XHAIR-01
  - XHAIR-02
  - XHAIR-03
  - XHAIR-04
  - XHAIR-05
  - XHAIR-06
  - XHAIR-07
  - XHAIR-08
  - XHAIR-09
  - EXPO-02

must_haves:
  truths:
    - "User sees a live SVG crosshair preview that updates when any control changes"
    - "User can switch between 4 crosshair styles and see controls hide/show per style"
    - "User can adjust size, thickness, gap via sliders with companion number inputs"
    - "User can set RGBA color via 4 sliders with live swatch preview"
    - "User can toggle center dot, T-style, outline, and follow recoil"
    - "User sees the CS2 import code at all times with a Copy Code button"
    - "Controls not applicable to selected style are hidden (display:none), not disabled"
    - "Style switching resets all parameters to CS2 defaults for that style"
  artifacts:
    - path: "src/features/crosshair/CrosshairDesigner.tsx"
      provides: "Page-level component composing all crosshair UI"
      exports: ["CrosshairDesigner"]
    - path: "src/features/crosshair/index.ts"
      provides: "Barrel export for CrosshairDesigner"
      exports: ["CrosshairDesigner"]
    - path: "src/App.tsx"
      provides: "App root mounting CrosshairDesigner"
  key_links:
    - from: "src/features/crosshair/CrosshairDesigner.tsx"
      to: "src/stores/crosshairStore.ts"
      via: "useCrosshairStore() for state + setters"
      pattern: "useCrosshairStore"
    - from: "src/features/crosshair/CrosshairDesigner.tsx"
      to: "src/features/crosshair/defaults.ts"
      via: "STYLE_DEFAULTS for style reset, STYLE_VISIBILITY for control visibility"
      pattern: "STYLE_DEFAULTS|STYLE_VISIBILITY"
    - from: "src/App.tsx"
      to: "src/features/crosshair/index.ts"
      via: "imports CrosshairDesigner"
      pattern: "CrosshairDesigner"
---

<objective>
Compose all crosshair components into the CrosshairDesigner page component, wire it into App.tsx, and verify the complete end-to-end crosshair workflow.

Purpose: Deliver the full user-facing crosshair designer that satisfies all XHAIR requirements — the final integration that makes every individual component work together as a cohesive feature.

Output: CrosshairDesigner page component, barrel export, App.tsx updated, all integration tests passing, visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crosshair-feature/02-CONTEXT.md
@.planning/phases/02-crosshair-feature/02-RESEARCH.md
@.planning/phases/02-crosshair-feature/02-UI-SPEC.md
@.planning/phases/02-crosshair-feature/02-02-SUMMARY.md
@.planning/phases/02-crosshair-feature/02-03-SUMMARY.md

<interfaces>
From src/features/crosshair/CrosshairPreview.tsx (Plan 02):
```typescript
export function CrosshairPreview(): JSX.Element;
// Reads useCrosshairStore directly. Renders SVG 200x200 viewBox -100,-100.
```

From src/features/crosshair/SegmentedButtonGroup.tsx (Plan 02):
```typescript
export function SegmentedButtonGroup({ activeValue, onChange }: {
  activeValue: number; onChange: (value: number) => void;
}): JSX.Element;
```

From src/features/crosshair/SectionCard.tsx (Plan 02):
```typescript
export function SectionCard({ heading, children, className? }: {
  heading: string; children: React.ReactNode; className?: string;
}): JSX.Element;
```

From src/features/crosshair/SliderRow.tsx (Plan 03):
```typescript
export function SliderRow({ label, min, max, step, value, onChange }: {
  label: string; min: number; max: number; step: number;
  value: number; onChange: (v: number) => void;
}): JSX.Element;
```

From src/features/crosshair/ToggleSwitch.tsx (Plan 03):
```typescript
export function ToggleSwitch({ label, checked, onChange }: {
  label: string; checked: boolean; onChange: (checked: boolean) => void;
}): JSX.Element;
```

From src/features/crosshair/ColorSection.tsx (Plan 03):
```typescript
export function ColorSection({ color, onColorChange }: {
  color: CrosshairColor; onColorChange: (partial: Partial<CrosshairColor>) => void;
}): JSX.Element;
```

From src/features/crosshair/ImportCodeBar.tsx (Plan 03):
```typescript
export function ImportCodeBar(): JSX.Element;
// Reads useCrosshairStore directly. Calls crosshairCodeGen. Renders CopyButton.
```

From src/features/crosshair/defaults.ts (Plan 01):
```typescript
export const STYLE_DEFAULTS: Record<number, CrosshairState>;
export const STYLE_VISIBILITY: Record<number, StyleVisibility>;
export const STYLE_OPTIONS: readonly [...];
```

From src/stores/crosshairStore.ts:
```typescript
interface CrosshairStore extends CrosshairState {
  setColor: (partial: Partial<CrosshairColor>) => void;
  setLine: (partial: Partial<CrosshairLine>) => void;
  setDot: (partial: Partial<CrosshairDot>) => void;
  setOutline: (partial: Partial<CrosshairOutline>) => void;
  set: (partial: Partial<CrosshairState>) => void;
  reset: () => void;
}
```

Slider Parameter Ranges (from UI-SPEC):
| Parameter | Store field | Min | Max | Step |
|-----------|-------------|-----|-----|------|
| Line length | line.length | 0 | 10 | 0.5 |
| Line thickness | line.thickness | 0.5 | 5 | 0.5 |
| Gap | line.gap | -5 | 5 | 0.5 |
| Outline thickness | outline.thickness | 0.5 | 3 | 0.5 |
| R | color.r | 0 | 255 | 1 |
| G | color.g | 0 | 255 | 1 |
| B | color.b | 0 | 255 | 1 |
| Alpha | color.a | 0 | 255 | 1 |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CrosshairDesigner page component, barrel export, and wire into App.tsx</name>
  <files>
    src/features/crosshair/CrosshairDesigner.tsx
    src/features/crosshair/index.ts
    src/App.tsx
    src/features/crosshair/__tests__/CrosshairDesigner.test.tsx
  </files>
  <read_first>
    src/App.tsx
    src/stores/crosshairStore.ts
    src/types/crosshair.ts
    src/features/crosshair/defaults.ts
    src/features/crosshair/CrosshairPreview.tsx
    src/features/crosshair/SegmentedButtonGroup.tsx
    src/features/crosshair/SectionCard.tsx
    src/features/crosshair/SliderRow.tsx
    src/features/crosshair/ToggleSwitch.tsx
    src/features/crosshair/ColorSection.tsx
    src/features/crosshair/ImportCodeBar.tsx
    src/features/crosshair/__tests__/CrosshairDesigner.test.tsx
    .planning/phases/02-crosshair-feature/02-UI-SPEC.md
    .planning/phases/02-crosshair-feature/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test: renders CrosshairPreview component (SVG element present)
    - Test: renders SegmentedButtonGroup with 4 style buttons
    - Test: renders Size and Shape section with 3 SliderRows (Line Length, Line Thickness, Gap)
    - Test: renders Color section with ColorSection component (4 RGBA sliders)
    - Test: renders Outline section with ToggleSwitch and conditional thickness slider
    - Test: hides Center Dot and T-Style toggles when style is Tee (5)
    - Test: shows Follow Recoil toggle only for Classic Dynamic (3)
    - Test: hides Follow Recoil for Classic Static (4), Classic (2), Tee (5)
    - Test: renders ImportCodeBar at the bottom
    - Test: style switch resets all params to STYLE_DEFAULTS
  </behavior>
  <action>
**CrosshairDesigner.tsx** — Create `src/features/crosshair/CrosshairDesigner.tsx`. This is the page-level composition component that owns all store reads (except CrosshairPreview and ImportCodeBar which read directly).

```tsx
import { useCrosshairStore } from '@/stores/crosshairStore';
import { STYLE_DEFAULTS, STYLE_VISIBILITY } from './defaults';
import { CrosshairPreview } from './CrosshairPreview';
import { SegmentedButtonGroup } from './SegmentedButtonGroup';
import { SectionCard } from './SectionCard';
import { SliderRow } from './SliderRow';
import { ToggleSwitch } from './ToggleSwitch';
import { ColorSection } from './ColorSection';
import { ImportCodeBar } from './ImportCodeBar';

export function CrosshairDesigner() {
  const { style, color, line, dot, outline, tStyle, followRecoil,
          setColor, setLine, setDot, setOutline, set } = useCrosshairStore();

  const visibility = STYLE_VISIBILITY[style] ?? STYLE_VISIBILITY[4];

  const handleStyleChange = (newStyle: number) => {
    const defaults = STYLE_DEFAULTS[newStyle];
    if (defaults) {
      set(defaults);
    }
  };

  return (
    <div className="min-h-screen bg-[#1a1a1a]">
      {/* Preview Panel */}
      <CrosshairPreview />

      {/* Controls Area */}
      <div className="max-w-[640px] mx-auto px-4 py-6 space-y-6">

        {/* Style Selector */}
        <SegmentedButtonGroup activeValue={style} onChange={handleStyleChange} />

        {/* Size & Shape Section */}
        <SectionCard heading="Size & Shape">
          <div className="space-y-0">
            <SliderRow label="Line Length" min={0} max={10} step={0.5} value={line.length}
              onChange={(v) => setLine({ length: v })} />
            <SliderRow label="Line Thickness" min={0.5} max={5} step={0.5} value={line.thickness}
              onChange={(v) => setLine({ thickness: v })} />
            <SliderRow label="Gap" min={-5} max={5} step={0.5} value={line.gap}
              onChange={(v) => setLine({ gap: v })} />
          </div>
          {/* Center Dot toggle — hidden for Tee (style 5) */}
          <div className={visibility.centerDot ? '' : 'hidden'}>
            <ToggleSwitch label="Center Dot" checked={dot.enabled}
              onChange={(v) => setDot({ enabled: v })} />
          </div>
          {/* T-Style toggle — hidden for Tee (style 5) */}
          <div className={visibility.tStyle ? '' : 'hidden'}>
            <ToggleSwitch label="T-Style" checked={tStyle}
              onChange={(v) => set({ tStyle: v })} />
          </div>
          {/* Follow Recoil toggle — visible only for Classic Dynamic (style 3) */}
          <div className={visibility.followRecoil ? '' : 'hidden'}>
            <ToggleSwitch label="Follow Recoil" checked={followRecoil}
              onChange={(v) => set({ followRecoil: v })} />
          </div>
        </SectionCard>

        {/* Color Section */}
        <SectionCard heading="Color">
          <ColorSection color={color} onColorChange={setColor} />
        </SectionCard>

        {/* Outline Section */}
        <SectionCard heading="Outline">
          <ToggleSwitch label="Outline" checked={outline.enabled}
            onChange={(v) => setOutline({ enabled: v })} />
          <div className={outline.enabled ? '' : 'hidden'}>
            <SliderRow label="Outline Thickness" min={0.5} max={3} step={0.5}
              value={outline.thickness}
              onChange={(v) => setOutline({ thickness: v })} />
          </div>
        </SectionCard>

        {/* Import Code Bar — always visible */}
        <ImportCodeBar />
      </div>
    </div>
  );
}
```

Key implementation details:
- **Style visibility**: Uses `STYLE_VISIBILITY[style]` to determine which toggles are shown. Hidden controls use `className="hidden"` (display:none per CONTEXT.md locked decision), NOT conditional rendering (Pitfall 4 from RESEARCH — unmounting loses local state).
- **Style reset**: `handleStyleChange` calls `set(STYLE_DEFAULTS[newStyle])` which resets ALL params including color (Pitfall 3 from RESEARCH — this is intentional per CONTEXT.md).
- **Controls layout**: max-width 640px centered, 16px horizontal padding, 24px (space-y-6) between sections per UI-SPEC.
- **Page background**: bg-[#1a1a1a] on the outer div (matches preview panel).
- **Slider ranges**: Exact min/max/step values from UI-SPEC Slider Parameter Ranges table.
- **Outline thickness slider**: Hidden when outline is disabled (same hidden pattern, not conditional render).

**index.ts** — Create `src/features/crosshair/index.ts`:
```typescript
export { CrosshairDesigner } from './CrosshairDesigner';
```

**App.tsx** — Update to mount CrosshairDesigner:
```tsx
import { CrosshairDesigner } from '@/features/crosshair';

function App() {
  return <CrosshairDesigner />;
}
export default App;
```

Remove the old placeholder div and the unused store imports (useCrosshairStore and useConfigStore were only there to verify store initialization in Phase 1 — CrosshairDesigner now imports useCrosshairStore internally). Keep useConfigStore import ONLY if tsconfig strict mode would complain about it being unused — actually, remove it since `noUnusedLocals: true` (Pitfall 6). The config store initializes lazily when first used in Phase 3.

**Tests** — Upgrade CrosshairDesigner.test.tsx from .todo stubs. Render `<CrosshairDesigner />`. Assert:
1. SVG element exists (CrosshairPreview rendered)
2. 4 style buttons exist by text content
3. "Line Length", "Line Thickness", "Gap" labels exist in the Size & Shape section
4. 4 range inputs exist in the Color section (R, G, B, Alpha)
5. "Outline" toggle exists
6. "CS2 Import Code" text exists (ImportCodeBar rendered)
7. For style visibility: set store to style 5 (Tee), then assert "Center Dot" label is in a hidden container. Set store to style 3 (Classic Dynamic), assert "Follow Recoil" label is visible.
8. For style reset: set store to style 4, change color to { r: 255, g: 0, b: 0, a: 255 }, switch to style 3 via clicking the button, assert color.r is back to default (0) by checking the R slider value.
  </action>
  <verify>
    <automated>npx vitest run src/features/crosshair/__tests__/CrosshairDesigner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/features/crosshair/CrosshairDesigner.tsx exists and exports CrosshairDesigner
    - CrosshairDesigner.tsx contains "useCrosshairStore" import
    - CrosshairDesigner.tsx contains "STYLE_DEFAULTS" and "STYLE_VISIBILITY" imports from "./defaults"
    - CrosshairDesigner.tsx contains "max-w-[640px]" on controls container
    - CrosshairDesigner.tsx contains "bg-[#1a1a1a]" on outer div
    - CrosshairDesigner.tsx contains "visibility.centerDot" for conditional hidden class
    - CrosshairDesigner.tsx contains "visibility.followRecoil" for conditional hidden class
    - CrosshairDesigner.tsx contains 'heading="Size & Shape"' and 'heading="Color"' and 'heading="Outline"'
    - CrosshairDesigner.tsx contains SliderRow with min={0} max={10} step={0.5} for Line Length
    - CrosshairDesigner.tsx contains SliderRow with min={-5} max={5} step={0.5} for Gap
    - CrosshairDesigner.tsx contains SliderRow with min={0.5} max={3} step={0.5} for Outline Thickness
    - src/features/crosshair/index.ts exists and contains 'export { CrosshairDesigner }'
    - src/App.tsx contains "CrosshairDesigner" import from "@/features/crosshair"
    - src/App.tsx does NOT contain "useConfigStore" (removed — unused, strict mode)
    - CrosshairDesigner.test.tsx has no remaining it.todo for the listed behaviors
    - npx vitest run src/features/crosshair/__tests__/CrosshairDesigner.test.tsx exits 0
  </acceptance_criteria>
  <done>CrosshairDesigner composes all components. Style visibility and reset work per CONTEXT.md. App.tsx mounts the designer. All integration tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete crosshair designer</name>
  <files>src/features/crosshair/CrosshairDesigner.tsx</files>
  <action>
Human visually verifies the complete crosshair designer in the browser. No code changes needed — this is a verification-only checkpoint after Task 1 automated all the implementation.
  </action>
  <what-built>
Complete crosshair designer with:
- Live SVG preview panel with dark background
- Style selector (4 buttons: Classic Static, Classic Dynamic, Classic, Tee)
- Size and Shape controls (line length, thickness, gap sliders)
- Color controls (R, G, B, Alpha sliders with color swatch)
- Toggle switches (center dot, T-style, follow recoil, outline)
- Outline thickness slider (conditional on outline toggle)
- CS2 Import Code bar with Copy Code button
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:5173
2. **Preview**: Verify green crosshair on dark (#1a1a1a) background at top
3. **Style switching**: Click each of the 4 style buttons. Verify:
   - Active button turns orange (#f97316)
   - All params reset to defaults (crosshair returns to default green, default size)
   - Tee style hides Center Dot and T-Style toggles
   - Classic Dynamic shows Follow Recoil toggle
   - Classic Static hides Follow Recoil
4. **Sliders**: Drag Line Length slider. Verify crosshair arms grow/shrink in real time. Verify number input updates as you drag. Type a value in the number input and press Enter — slider should update.
5. **Color**: Drag R slider to 255, G to 0. Verify crosshair turns red. Verify color swatch updates. Verify RGBA readout text updates.
6. **Alpha**: Drag Alpha toward 0. Verify crosshair becomes transparent in the preview.
7. **Toggles**: Enable Center Dot — verify dot appears at crosshair center. Enable T-Style — verify top arm disappears. Enable Outline — verify black outline appears around arms, and outline thickness slider becomes visible.
8. **Import Code**: Verify CSGO-XXXXX-... code is visible at the bottom. Click "Copy Code" — verify button text changes to "Copied!" for 2 seconds. Paste clipboard content — it should be the CSGO- code.
9. **Responsive**: Narrow browser window to ~375px. Verify controls stack properly, no horizontal overflow.
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (production build succeeds)
    - Human confirms crosshair preview renders correctly
    - Human confirms style switching works with control visibility
    - Human confirms sliders update preview in real time
    - Human confirms Copy Code button copies CSGO- share code
  </acceptance_criteria>
  <done>Human approves the visual and functional quality of the complete crosshair designer.</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User slider input to store | All parameter values flow through Zustand setters |
| Store to SVG render | Crosshair state rendered as SVG attributes |
| Store to clipboard | Share code string generated and copied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | CrosshairDesigner style reset | accept | Style reset calls `set(STYLE_DEFAULTS[n])` with hardcoded values from defaults.ts. No user-controlled path can inject arbitrary state into the reset. |
| T-02-07 | Tampering | Hidden controls via className | accept | Hidden controls use `className="hidden"` (CSS display:none). The underlying input values remain in store but are not rendered. Toggling visibility cannot corrupt state — values persist correctly. |
| T-02-08 | Denial of Service | Rapid slider dragging | accept | All computation is synchronous and O(1). No network calls, no heavy computation. React batches state updates. Performance is not a concern for this component tree size. |
</threat_model>

<verification>
1. `npx vitest run src/features/crosshair/__tests__/` — all 7 test files pass
2. `npx vitest run` — full suite passes (including Phase 1 tests)
3. `npx tsc --noEmit` exits 0
4. `npm run build` produces production bundle with no errors
5. Visual checkpoint confirms end-to-end crosshair workflow
</verification>

<success_criteria>
- CrosshairDesigner composes all 8 child components into a single page
- Style switching resets all params and shows/hides controls per STYLE_VISIBILITY
- All slider ranges match UI-SPEC (line length 0-10/0.5, thickness 0.5-5/0.5, gap -5..5/0.5, outline 0.5-3/0.5)
- Controls area is max-width 640px centered with 16px padding and 24px section gaps
- App.tsx mounts CrosshairDesigner as the sole page content
- Import code bar is always visible at the bottom
- All automated tests pass, visual checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-crosshair-feature/02-04-SUMMARY.md`
</output>
