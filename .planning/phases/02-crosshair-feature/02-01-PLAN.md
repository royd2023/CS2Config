---
phase: 02-crosshair-feature
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/features/crosshair/__tests__/CrosshairPreview.test.tsx
  - src/features/crosshair/__tests__/SegmentedButtonGroup.test.tsx
  - src/features/crosshair/__tests__/SliderRow.test.tsx
  - src/features/crosshair/__tests__/ColorSection.test.tsx
  - src/features/crosshair/__tests__/ToggleSwitch.test.tsx
  - src/features/crosshair/__tests__/ImportCodeBar.test.tsx
  - src/features/crosshair/__tests__/CrosshairDesigner.test.tsx
  - src/features/crosshair/defaults.ts
  - src/index.css
  - src/components/ui/Slider.tsx
  - src/components/ui/CopyButton.tsx
  - src/components/ui/index.ts
autonomous: true
requirements:
  - XHAIR-01
  - XHAIR-02
  - XHAIR-03
  - XHAIR-04
  - XHAIR-05
  - XHAIR-06
  - XHAIR-07
  - XHAIR-08
  - XHAIR-09
  - EXPO-02

must_haves:
  truths:
    - "All 7 test stub files exist and are importable by Vitest"
    - "Per-style default crosshair values are defined for styles 2, 3, 4, 5"
    - "Slider and CopyButton primitives accept a className prop"
    - "Range input and toggle switch CSS classes are defined in index.css"
  artifacts:
    - path: "src/features/crosshair/__tests__/CrosshairPreview.test.tsx"
      provides: "Test stubs for XHAIR-01, XHAIR-09"
    - path: "src/features/crosshair/__tests__/SegmentedButtonGroup.test.tsx"
      provides: "Test stubs for XHAIR-02"
    - path: "src/features/crosshair/__tests__/SliderRow.test.tsx"
      provides: "Test stubs for XHAIR-03"
    - path: "src/features/crosshair/__tests__/ColorSection.test.tsx"
      provides: "Test stubs for XHAIR-04"
    - path: "src/features/crosshair/__tests__/ToggleSwitch.test.tsx"
      provides: "Test stubs for XHAIR-05, XHAIR-06"
    - path: "src/features/crosshair/__tests__/ImportCodeBar.test.tsx"
      provides: "Test stubs for XHAIR-08, EXPO-02"
    - path: "src/features/crosshair/__tests__/CrosshairDesigner.test.tsx"
      provides: "Test stubs for XHAIR-07"
    - path: "src/features/crosshair/defaults.ts"
      provides: "STYLE_DEFAULTS and STYLE_VISIBILITY maps"
    - path: "src/index.css"
      provides: "slider-range and toggle CSS classes"
  key_links:
    - from: "src/features/crosshair/defaults.ts"
      to: "src/types/crosshair.ts"
      via: "imports CrosshairState type"
      pattern: "import.*CrosshairState.*from.*@/types/crosshair"
---

<objective>
Create the Wave 0 foundation for Phase 2: test stub files for all 7 crosshair components, per-style default values, CSS custom classes for range inputs and toggle switches, and className prop upgrades for Phase 1 primitives.

Purpose: Establish test scaffolding before any component work begins (Nyquist requirement), define the data contracts (defaults, visibility maps) that all Wave 1 components depend on, and lay the CSS foundation for styled form inputs.

Output: 7 test stub files (all pending/skipped), defaults.ts with STYLE_DEFAULTS + STYLE_VISIBILITY, updated index.css with slider-range and toggle-switch classes, Slider and CopyButton with className pass-through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crosshair-feature/02-CONTEXT.md
@.planning/phases/02-crosshair-feature/02-RESEARCH.md
@.planning/phases/02-crosshair-feature/02-UI-SPEC.md
@.planning/phases/02-crosshair-feature/02-VALIDATION.md

<interfaces>
From src/types/crosshair.ts:
```typescript
export interface CrosshairColor { r: number; g: number; b: number; a: number; }
export interface CrosshairLine { length: number; thickness: number; gap: number; }
export interface CrosshairDot { enabled: boolean; }
export interface CrosshairOutline { enabled: boolean; thickness: number; }
export type CrosshairStyle = 0 | 1 | 2 | 3 | 4 | 5;
export interface CrosshairState {
  style: CrosshairStyle;
  color: CrosshairColor;
  line: CrosshairLine;
  dot: CrosshairDot;
  outline: CrosshairOutline;
  tStyle: boolean;
  followRecoil: boolean;
}
```

From src/stores/crosshairStore.ts:
```typescript
export const DEFAULT_CROSSHAIR: CrosshairState = {
  style: 4, color: { r: 0, g: 255, b: 0, a: 200 },
  line: { length: 4, thickness: 1, gap: -3 },
  dot: { enabled: false }, outline: { enabled: false, thickness: 1 },
  tStyle: false, followRecoil: false,
};
export const useCrosshairStore = create<CrosshairStore>()((set) => ({ ... }));
```

From src/components/ui/Slider.tsx:
```typescript
interface SliderProps {
  label: string; min: number; max: number; step?: number;
  value: number; onChange: (value: number) => void;
}
export function Slider({ label, min, max, step = 1, value, onChange }: SliderProps) { ... }
```

From src/components/ui/CopyButton.tsx:
```typescript
interface CopyButtonProps { value: string; label?: string; }
export function CopyButton({ value, label = 'Copy' }: CopyButtonProps) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 7 Wave 0 test stub files</name>
  <files>
    src/features/crosshair/__tests__/CrosshairPreview.test.tsx
    src/features/crosshair/__tests__/SegmentedButtonGroup.test.tsx
    src/features/crosshair/__tests__/SliderRow.test.tsx
    src/features/crosshair/__tests__/ColorSection.test.tsx
    src/features/crosshair/__tests__/ToggleSwitch.test.tsx
    src/features/crosshair/__tests__/ImportCodeBar.test.tsx
    src/features/crosshair/__tests__/CrosshairDesigner.test.tsx
  </files>
  <read_first>
    src/types/crosshair.ts
    src/stores/crosshairStore.ts
    .planning/phases/02-crosshair-feature/02-VALIDATION.md
    .planning/phases/02-crosshair-feature/02-UI-SPEC.md
  </read_first>
  <action>
Create the directory `src/features/crosshair/__tests__/` and all 7 test files. Each file contains `describe` blocks with `it.todo()` stubs that document the exact behaviors to be tested. Use `@testing-library/react` imports and `vitest` globals (already configured with `globals: true`).

**CrosshairPreview.test.tsx** stubs:
- `it.todo('renders an SVG element with viewBox="-100 -100 200 200"')` — XHAIR-01
- `it.todo('renders 4 rect arms when tStyle is false')` — XHAIR-01
- `it.todo('renders 3 rect arms when tStyle is true (top arm omitted)')` — XHAIR-06
- `it.todo('renders center dot rect when dot.enabled is true')` — XHAIR-05
- `it.todo('renders no center dot rect when dot.enabled is false')` — XHAIR-05
- `it.todo('renders outline rects when outline.enabled is true')` — XHAIR-07
- `it.todo('applies rgba fill from store color values')` — XHAIR-04
- `it.todo('wrapping div has bg-[#1a1a1a] class')` — XHAIR-09
- `it.todo('updates SVG when store state changes')` — XHAIR-01

**SegmentedButtonGroup.test.tsx** stubs:
- `it.todo('renders 4 buttons with labels Classic Static, Classic Dynamic, Classic, Tee')` — XHAIR-02
- `it.todo('highlights the active button with bg-[#f97316]')` — XHAIR-02
- `it.todo('calls onChange with style value when button clicked')` — XHAIR-02
- `it.todo('maps button positions to style values: 4, 3, 2, 5')` — XHAIR-02

**SliderRow.test.tsx** stubs:
- `it.todo('renders a label, range input, and number input')` — XHAIR-03
- `it.todo('calls onChange when range slider value changes')` — XHAIR-03
- `it.todo('clamps number input to min/max on blur')` — XHAIR-03
- `it.todo('syncs number input display when value prop changes')` — XHAIR-03
- `it.todo('updates on Enter key press in number input')` — XHAIR-03

**ColorSection.test.tsx** stubs:
- `it.todo('renders 4 slider rows for R, G, B, Alpha')` — XHAIR-04
- `it.todo('renders a color swatch showing current RGBA')` — XHAIR-04
- `it.todo('calls onColorChange with updated r value when R slider changes')` — XHAIR-04
- `it.todo('renders RGBA readout text in mono font')` — XHAIR-04

**ToggleSwitch.test.tsx** stubs:
- `it.todo('renders a checkbox input (sr-only) and a visible toggle track')` — XHAIR-05
- `it.todo('calls onChange(true) when toggled on')` — XHAIR-05
- `it.todo('calls onChange(false) when toggled off')` — XHAIR-05
- `it.todo('shows accent color track when checked')` — XHAIR-05
- `it.todo('clicking the label text also toggles the switch')` — XHAIR-05

**ImportCodeBar.test.tsx** stubs:
- `it.todo('renders the CS2 Import Code label in uppercase')` — XHAIR-08
- `it.todo('renders the share code string from crosshairCodeGen')` — XHAIR-08
- `it.todo('renders a CopyButton with label Copy Code')` — EXPO-02
- `it.todo('CopyButton receives the generated share code as value')` — EXPO-02

**CrosshairDesigner.test.tsx** stubs:
- `it.todo('renders CrosshairPreview component')` — XHAIR-01
- `it.todo('renders SegmentedButtonGroup for style selection')` — XHAIR-02
- `it.todo('renders Size and Shape section with SliderRow controls')` — XHAIR-03
- `it.todo('renders Color section with ColorSection component')` — XHAIR-04
- `it.todo('renders Outline section with toggle and conditional thickness slider')` — XHAIR-07
- `it.todo('hides center dot toggle when style is Tee (5)')` — XHAIR-07
- `it.todo('shows follow recoil toggle only for Classic Dynamic (3)')` — XHAIR-07
- `it.todo('renders ImportCodeBar at the bottom')` — XHAIR-08

Each file must import `{ describe, it }` from 'vitest' if globals don't cover the types (they do — `globals: true` is set, so no import needed). Import `{ render, screen }` from `@testing-library/react` at the top even though stubs don't use them yet — this validates the import path.
  </action>
  <verify>
    <automated>npx vitest run src/features/crosshair/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/crosshair/__tests__/CrosshairPreview.test.tsx exists and contains "viewBox" and "bg-[#1a1a1a]"
    - src/features/crosshair/__tests__/SegmentedButtonGroup.test.tsx exists and contains "Classic Static" and "Classic Dynamic"
    - src/features/crosshair/__tests__/SliderRow.test.tsx exists and contains "clamps number input"
    - src/features/crosshair/__tests__/ColorSection.test.tsx exists and contains "R, G, B, Alpha"
    - src/features/crosshair/__tests__/ToggleSwitch.test.tsx exists and contains "onChange(true)"
    - src/features/crosshair/__tests__/ImportCodeBar.test.tsx exists and contains "Copy Code"
    - src/features/crosshair/__tests__/CrosshairDesigner.test.tsx exists and contains "CrosshairPreview" and "ImportCodeBar"
    - npx vitest run src/features/crosshair/__tests__/ exits 0 (all tests are .todo so none fail)
  </acceptance_criteria>
  <done>All 7 test files created with .todo stubs covering all XHAIR and EXPO-02 requirements. Vitest runs them with exit code 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create defaults.ts with STYLE_DEFAULTS and STYLE_VISIBILITY</name>
  <files>
    src/features/crosshair/defaults.ts
  </files>
  <read_first>
    src/types/crosshair.ts
    src/stores/crosshairStore.ts
    .planning/phases/02-crosshair-feature/02-RESEARCH.md
    .planning/phases/02-crosshair-feature/02-UI-SPEC.md
  </read_first>
  <action>
Create `src/features/crosshair/defaults.ts` with two exported constants:

**STYLE_DEFAULTS** — a `Record<number, CrosshairState>` mapping style IDs to full CrosshairState objects. Import `CrosshairState` from `@/types/crosshair`. Values:

```typescript
export const STYLE_DEFAULTS: Record<number, CrosshairState> = {
  4: { style: 4, color: { r: 0, g: 255, b: 0, a: 200 }, line: { length: 4, thickness: 1, gap: -3 }, dot: { enabled: false }, outline: { enabled: false, thickness: 1 }, tStyle: false, followRecoil: false },
  3: { style: 3, color: { r: 0, g: 255, b: 0, a: 200 }, line: { length: 4, thickness: 1, gap: 0 }, dot: { enabled: false }, outline: { enabled: false, thickness: 1 }, tStyle: false, followRecoil: false },
  2: { style: 2, color: { r: 0, g: 255, b: 0, a: 200 }, line: { length: 4, thickness: 1, gap: 0 }, dot: { enabled: false }, outline: { enabled: false, thickness: 1 }, tStyle: false, followRecoil: false },
  5: { style: 5, color: { r: 0, g: 255, b: 0, a: 200 }, line: { length: 4, thickness: 1, gap: 0 }, dot: { enabled: false }, outline: { enabled: false, thickness: 1 }, tStyle: false, followRecoil: false },
};
```

**STYLE_VISIBILITY** — controls which UI controls are visible per style. From UI-SPEC Style-to-Parameter Visibility Map:

```typescript
export interface StyleVisibility {
  centerDot: boolean;
  tStyle: boolean;
  followRecoil: boolean;
}

export const STYLE_VISIBILITY: Record<number, StyleVisibility> = {
  4: { centerDot: true,  tStyle: true,  followRecoil: false },  // Classic Static
  3: { centerDot: true,  tStyle: true,  followRecoil: true  },  // Classic Dynamic
  2: { centerDot: true,  tStyle: true,  followRecoil: false },  // Classic
  5: { centerDot: false, tStyle: false, followRecoil: false },  // Tee
};
```

**STYLE_OPTIONS** — the 4 style button labels and values for the segmented button group:

```typescript
export const STYLE_OPTIONS = [
  { label: 'Classic Static', value: 4 },
  { label: 'Classic Dynamic', value: 3 },
  { label: 'Classic', value: 2 },
  { label: 'Tee', value: 5 },
] as const;
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/crosshair/defaults.ts exists
    - File contains "export const STYLE_DEFAULTS" with keys 4, 3, 2, 5
    - File contains "export const STYLE_VISIBILITY" with keys 4, 3, 2, 5
    - File contains "export const STYLE_OPTIONS" with "Classic Static", "Classic Dynamic", "Classic", "Tee"
    - File contains "import type { CrosshairState } from '@/types/crosshair'"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>defaults.ts exports STYLE_DEFAULTS (4 style presets), STYLE_VISIBILITY (per-style control visibility), and STYLE_OPTIONS (segmented button config). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS classes for slider and toggle, upgrade Slider and CopyButton with className prop</name>
  <files>
    src/index.css
    src/components/ui/Slider.tsx
    src/components/ui/CopyButton.tsx
    src/components/ui/index.ts
  </files>
  <read_first>
    src/index.css
    src/components/ui/Slider.tsx
    src/components/ui/CopyButton.tsx
    src/components/ui/index.ts
    .planning/phases/02-crosshair-feature/02-RESEARCH.md
    .planning/phases/02-crosshair-feature/02-UI-SPEC.md
  </read_first>
  <action>
**1. Update `src/index.css`** — add after `@import "tailwindcss";`:

```css
@layer components {
  .slider-range {
    -webkit-appearance: none;
    appearance: none;
    height: 4px;
    border-radius: 2px;
    background: #3a3a3a;
    outline: none;
    cursor: pointer;
    width: 100%;
  }
  .slider-range::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: #e5e5e5;
    cursor: pointer;
  }
  .slider-range::-moz-range-thumb {
    width: 16px;
    height: 16px;
    border-radius: 50%;
    background: #e5e5e5;
    border: none;
    cursor: pointer;
  }
  .slider-range:focus-visible::-webkit-slider-thumb {
    outline: 2px solid #f97316;
    outline-offset: 2px;
  }
  .slider-range:focus-visible::-moz-range-thumb {
    outline: 2px solid #f97316;
    outline-offset: 2px;
  }
  .number-input {
    width: 56px;
    text-align: right;
    font-size: 14px;
    background: #1a1a1a;
    border: 1px solid #3a3a3a;
    border-radius: 4px;
    color: #e5e5e5;
    padding: 4px 6px;
  }
  .number-input:focus {
    outline: 2px solid #f97316;
    outline-offset: 0;
  }
}
```

**2. Update `src/components/ui/Slider.tsx`** — add optional `className?: string` to the `SliderProps` interface. Pass it to the `<input type="range">` element. The full updated component:

```tsx
interface SliderProps {
  label: string;
  min: number;
  max: number;
  step?: number;
  value: number;
  onChange: (value: number) => void;
  className?: string;
}

export function Slider({ label, min, max, step = 1, value, onChange, className }: SliderProps) {
  return (
    <div>
      <label>
        {label}: {value}
        <input
          type="range"
          min={min}
          max={max}
          step={step}
          value={value}
          onChange={(e) => onChange(Number(e.target.value))}
          className={className}
        />
      </label>
    </div>
  );
}
```

**3. Update `src/components/ui/CopyButton.tsx`** — add optional `className?: string` to the `CopyButtonProps` interface. Pass it to the `<button>` element:

```tsx
interface CopyButtonProps {
  value: string;
  label?: string;
  className?: string;
}

export function CopyButton({ value, label = 'Copy', className }: CopyButtonProps) {
  // ... same implementation, just add className to <button>:
  return (
    <button onClick={handleCopy} type="button" className={className}>
      {copied ? 'Copied!' : label}
    </button>
  );
}
```

**4. Verify `src/components/ui/index.ts`** barrel export still re-exports both. No change expected unless it's missing exports — just confirm it has `export { Slider }` and `export { CopyButton }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains ".slider-range" class with "-webkit-appearance: none"
    - src/index.css contains ".slider-range::-webkit-slider-thumb" with "background: #e5e5e5"
    - src/index.css contains ".slider-range:focus-visible::-webkit-slider-thumb" with "#f97316"
    - src/index.css contains ".number-input" with "width: 56px" and "background: #1a1a1a"
    - src/components/ui/Slider.tsx contains "className?: string" in the interface
    - src/components/ui/Slider.tsx contains "className={className}" on the input element
    - src/components/ui/CopyButton.tsx contains "className?: string" in the interface
    - src/components/ui/CopyButton.tsx contains "className={className}" on the button element
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CSS slider-range and number-input classes defined in index.css. Slider and CopyButton accept className prop. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Wave 0 creates test stubs, data constants, and CSS only — no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00 | N/A | Wave 0 artifacts | accept | No user input processed; test stubs, static data, CSS classes only |
</threat_model>

<verification>
1. `npx vitest run src/features/crosshair/__tests__/` exits 0 (all .todo stubs pass)
2. `npx tsc --noEmit` exits 0 (defaults.ts types check, primitives compile)
3. `npm run dev` starts without errors (CSS doesn't break Vite)
</verification>

<success_criteria>
- 7 test stub files in src/features/crosshair/__tests__/ with .todo stubs for all requirements
- defaults.ts exports STYLE_DEFAULTS, STYLE_VISIBILITY, and STYLE_OPTIONS
- index.css has slider-range and number-input CSS classes
- Slider and CopyButton primitives accept className prop
- Full test suite passes, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-crosshair-feature/02-01-SUMMARY.md`
</output>
