// src/features/crosshair/__tests__/ColorSection.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it } from 'vitest';

describe('ColorSection', () => {
  it.todo('renders 4 slider rows for R, G, B, Alpha');
  it.todo('renders a color swatch showing current RGBA');
  it.todo('calls onColorChange with updated r value when R slider changes');
  it.todo('renders RGBA readout text in mono font');
});
