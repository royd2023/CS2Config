// src/features/crosshair/__tests__/SegmentedButtonGroup.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it } from 'vitest';

describe('SegmentedButtonGroup', () => {
  it.todo('renders 4 buttons with labels Classic Static, Classic Dynamic, Classic, Tee');
  it.todo('highlights the active button with bg-[#f97316]');
  it.todo('calls onChange with style value when button clicked');
  it.todo('maps button positions to style values: 4, 3, 2, 5');
});
