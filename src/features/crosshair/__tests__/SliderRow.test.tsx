// src/features/crosshair/__tests__/SliderRow.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it } from 'vitest';

describe('SliderRow', () => {
  it.todo('renders a label, range input, and number input');
  it.todo('calls onChange when range slider value changes');
  it.todo('clamps number input to min/max on blur');
  it.todo('syncs number input display when value prop changes');
  it.todo('updates on Enter key press in number input');
});
