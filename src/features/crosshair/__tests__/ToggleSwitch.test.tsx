// src/features/crosshair/__tests__/ToggleSwitch.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it } from 'vitest';

describe('ToggleSwitch', () => {
  it.todo('renders a checkbox input (sr-only) and a visible toggle track');
  it.todo('calls onChange(true) when toggled on');
  it.todo('calls onChange(false) when toggled off');
  it.todo('shows accent color track when checked');
  it.todo('clicking the label text also toggles the switch');
});
